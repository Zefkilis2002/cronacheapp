const config = {
  // Sostituisci questo URL con l'indirizzo del tuo backend pubblicato quando sei in produzione
  // Es: 'https://il-tuo-backend.render.com'
  API_BASE_URL: 'https://cronacheapp.web.app/'
};

export default config;